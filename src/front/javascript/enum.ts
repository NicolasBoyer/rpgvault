export enum EInterface {
    hover = 'hover',
    movable = 'movable',
    hidden = 'hidden',
}

export enum EElementType {
    image = 'image',
    input = 'input',
}

export enum EHistoryPosition {
    up = 'up',
    down = 'down',
}
